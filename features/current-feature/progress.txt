# Animation Feature Progress

## PRD-006 - State - To Generator Params [DONE]

**Files changed:**
- src/hooks/useFragmentState.ts - added toParams/setToParams state + debouncedToParams + initialization effect
- src/lib/urlState.ts - added GeneratorParamsUrl type, to_* URL keys, serialization/deserialization

**Key decisions:**
- toParams type is GeneratorParams | null (null = uninitialized)
- URL uses to_* prefix for all To param keys (e.g. to_t, to_f, to_s)
- Initialization: copies from params, sets random frequency (0.01-0.50)
- toParams persists across animation enable/disable (null only on first init)

**Notes for next iteration:**
- PRD-007 (Initialization) is partially done via the init effect, but UI needs To panel first
- PRD-008 (Remove old seeds) should wait until new system fully working
- Start with UI changes (PRD-003/004 for From/To panels) since state is ready

## PRD-003 - UI - From Panel [DONE]

**Files changed:**
- src/app/components/fragment/ParametersPanel.tsx - added optional `title` prop, defaults to "Parameters"
- src/app/components/AssetGenerator.tsx - pass title="From" when animationEnabled, else "Parameters"

**Key decisions:**
- Minimal change: just add title prop rather than creating separate FromPanel component
- Title changes dynamically based on state.animationEnabled
- All existing param controls remain unchanged (already has density, fill, gamma, frequency, contrast, directional neighbors, direction density)

**Notes for next iteration:**
- PRD-004 (To Panel) is next - reuse ParametersPanel with title="To" and toParams state
- PRD-002 (Colors position) already satisfied - Colors already above parameters in current layout

## PRD-004 - UI - To Panel [DONE]

**Files changed:**
- src/app/components/AssetGenerator.tsx - added conditional render of second ParametersPanel for To params
- src/app/components/fragment/ParametersPanel.tsx - changed setParams type to simple callback for compatibility with nullable state setter

**Key decisions:**
- Reuse existing ParametersPanel component with title="To"
- Panel only renders when both animationEnabled and toParams are truthy
- Changed ParametersPanelProps.setParams from Dispatch<SetStateAction<GeneratorParams>> to simple callback type (params: GeneratorParams) => void for compatibility with setToParams which handles nullable type

**Notes for next iteration:**
- PRD-007 (Initialization) logic already exists in useFragmentState - should verify behavior via UI
- PRD-009 (SVG Generation) needed to actually see diff preview update
- PRD-002 (Colors position) appears satisfied - Colors already above From/To panels in current layout

## PRD-009 - SVG Generation - Dual Config [DONE]

**Files changed:**
- src/lib/generateFragmentSvg.ts - added GenerateFragmentDiffFromConfigsOptions type + generateFragmentDiffFromConfigs function
- src/lib/generateFragmentSvgGrid.ts - re-export new type and function
- src/hooks/useFragmentGeneration.ts - updated diffSvg memo to use dual-config approach when toParams available

**Key decisions:**
- Created new function generateFragmentDiffFromConfigs (kept old generateFragmentDiffSvg for backwards compat)
- New function takes fromConfig and toConfig (both Omit<FragmentConfig, 'seedParam'>)
- Canvas/color settings taken from fromConfig (shared settings per PRD)
- Same data-g attribute scheme: shared=none, from-only=data-g="a", to-only=data-g="b" opacity:0
- Hook prioritizes toParams when available, falls back to old seed approach for legacy

**Notes for next iteration:**
- PRD-010 (Hover Behavior) should work unchanged since data-g scheme is same
- PRD-008 (Remove old seeds) can now be done since new system is functional
- VM cannot run build/typecheck due to esbuild arch issue - needs manual verification
