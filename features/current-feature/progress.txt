# Animation Feature Progress

## PRD-006 - State - To Generator Params [DONE]

**Files changed:**
- src/hooks/useFragmentState.ts - added toParams/setToParams state + debouncedToParams + initialization effect
- src/lib/urlState.ts - added GeneratorParamsUrl type, to_* URL keys, serialization/deserialization

**Key decisions:**
- toParams type is GeneratorParams | null (null = uninitialized)
- URL uses to_* prefix for all To param keys (e.g. to_t, to_f, to_s)
- Initialization: copies from params, sets random frequency (0.01-0.50)
- toParams persists across animation enable/disable (null only on first init)

**Notes for next iteration:**
- PRD-007 (Initialization) is partially done via the init effect, but UI needs To panel first
- PRD-008 (Remove old seeds) should wait until new system fully working
- Start with UI changes (PRD-003/004 for From/To panels) since state is ready

## PRD-003 - UI - From Panel [DONE]

**Files changed:**
- src/app/components/fragment/ParametersPanel.tsx - added optional `title` prop, defaults to "Parameters"
- src/app/components/AssetGenerator.tsx - pass title="From" when animationEnabled, else "Parameters"

**Key decisions:**
- Minimal change: just add title prop rather than creating separate FromPanel component
- Title changes dynamically based on state.animationEnabled
- All existing param controls remain unchanged (already has density, fill, gamma, frequency, contrast, directional neighbors, direction density)

**Notes for next iteration:**
- PRD-004 (To Panel) is next - reuse ParametersPanel with title="To" and toParams state
- PRD-002 (Colors position) already satisfied - Colors already above parameters in current layout

## PRD-004 - UI - To Panel [DONE]

**Files changed:**
- src/app/components/AssetGenerator.tsx - added conditional render of second ParametersPanel for To params
- src/app/components/fragment/ParametersPanel.tsx - changed setParams type to simple callback for compatibility with nullable state setter

**Key decisions:**
- Reuse existing ParametersPanel component with title="To"
- Panel only renders when both animationEnabled and toParams are truthy
- Changed ParametersPanelProps.setParams from Dispatch<SetStateAction<GeneratorParams>> to simple callback type (params: GeneratorParams) => void for compatibility with setToParams which handles nullable type

**Notes for next iteration:**
- PRD-007 (Initialization) logic already exists in useFragmentState - should verify behavior via UI
- PRD-009 (SVG Generation) needed to actually see diff preview update
- PRD-002 (Colors position) appears satisfied - Colors already above From/To panels in current layout

## PRD-009 - SVG Generation - Dual Config [DONE]

**Files changed:**
- src/lib/generateFragmentSvg.ts - added GenerateFragmentDiffFromConfigsOptions type + generateFragmentDiffFromConfigs function
- src/lib/generateFragmentSvgGrid.ts - re-export new type and function
- src/hooks/useFragmentGeneration.ts - updated diffSvg memo to use dual-config approach when toParams available

**Key decisions:**
- Created new function generateFragmentDiffFromConfigs (kept old generateFragmentDiffSvg for backwards compat)
- New function takes fromConfig and toConfig (both Omit<FragmentConfig, 'seedParam'>)
- Canvas/color settings taken from fromConfig (shared settings per PRD)
- Same data-g attribute scheme: shared=none, from-only=data-g="a", to-only=data-g="b" opacity:0
- Hook prioritizes toParams when available, falls back to old seed approach for legacy

**Notes for next iteration:**
- PRD-010 (Hover Behavior) should work unchanged since data-g scheme is same
- PRD-008 (Remove old seeds) can now be done since new system is functional
- VM cannot run build/typecheck due to esbuild arch issue - needs manual verification

## PRD-008 + PRD-001 + PRD-011/012/013/015 - Remove Old Seeds & Update Export/Import [DONE]

**Files changed:**
- src/hooks/useFragmentState.ts - removed animationSeedA/B state, setters, debounced state, auto-gen effect, URL sync
- src/lib/urlState.ts - removed animationSeedA/B from type, PARAM_KEYS, DEFAULTS, serialization, parsing
- src/app/components/fragment/AnimationPanel.tsx - removed seed inputs + Randomize Seeds button (PRD-001)
- src/hooks/useFragmentActions.ts - updated resetToDefaults to clear toParams, updated export to use toConfig, updated import to handle toConfig
- src/hooks/useFragmentGeneration.ts - removed legacy seed-based fallback, only use dual-config approach

**Key decisions:**
- Full removal of animationSeedA/B - no backwards compat in state (toParams replaces it)
- Export JSON: presence of toConfig implies animation enabled (no animationEnabled field needed)
- Import JSON: if toConfig exists -> enable animation + populate toParams; else -> disable animation
- Legacy animationSeedA/B fields in import are ignored gracefully (comment documents this)
- URL params: removed sa/sb keys entirely

**PRDs completed in this batch:**
- PRD-001: Seed UI removed from AnimationPanel
- PRD-008: animationSeedA/B state removed
- PRD-011: Export uses toConfig format
- PRD-012: Import handles toConfig + legacy gracefully
- PRD-013: URL state uses to_* params only (sa/sb removed)
- PRD-015: Reset to Defaults clears toParams

**Notes for next iteration:**
- PRD-002 (Colors position) - verify layout in browser
- PRD-005 (Shared Canvas Settings) - verify behavior
- PRD-007 (Initialization) - verify toParams init when animation first enabled
- PRD-010 (Hover Behavior) - verify hover works with dual-config SVG
- PRD-014 (Per-panel Randomize) - needs implementation
- PRD-007 (Initialization) - verify toParams init when animation first enabled
- PRD-010 (Hover Behavior) - verify hover works with dual-config SVG
- PRD-014 (Per-panel Randomize) - needs implementation
- VM cannot run build/typecheck - needs manual verification on host machine

## PRD-016 + PRD-002 + PRD-005 - Sidebar Layout & Shared Sections [DONE]

**Files changed:**
- src/app/components/AssetGenerator.tsx - wider sidebar + grid layout for From/To panels

**Key decisions:**
- Sidebar width: 400px (default) → 750px (animation enabled) with smooth transition
- From/To panels in `grid grid-cols-2 gap-4` when animation enabled
- Shared sections (CanvasSettingsPanel, AnimationPanel, ColorsPanel) always full-width above panels
- Colors panel already above From/To panels → PRD-002 satisfied
- Canvas settings already single instance → PRD-005 satisfied

**Implementation:**
- Dynamic class: `${state.animationEnabled ? 'w-[750px]' : 'w-[400px]'}`
- Transition: `transition-all duration-300` for smooth width change
- Grid container wraps From/To panels only when animation enabled
- Single ParametersPanel shown when animation disabled

## PRD-014 - Actions - Randomize [DONE]

**Files changed:**
- src/hooks/useFragmentActions.ts - extracted generateRandomParams(), added randomizeToParams action
- src/app/components/fragment/ParametersPanel.tsx - added onRandomize prop, renders Shuffle button in header when provided
- src/app/components/fragment/ActionButtons.tsx - added animationEnabled prop, conditionally hides global Randomize button
- src/app/components/AssetGenerator.tsx - passes onRandomize to From/To panels, passes animationEnabled to ActionButtons

**Key decisions:**
- Extracted param generation to shared generateRandomParams() function (DRY)
- Per-panel Randomize buttons appear in panel header next to title
- Global Randomize button hidden via `{!animationEnabled && (...)}` conditional
- When animation disabled, no onRandomize passed to ParametersPanel → no button shown

**Notes for next iteration:**
- PRD-007 (Initialization) - needs verification
- PRD-010 (Hover Behavior) - needs verification
- VM cannot run build/typecheck - needs manual verification on host machine
