# Feature Progress Log

## 2026-02-06 - PRD-001, PRD-002, PRD-003, PRD-004, PRD-006

### Completed
- Removed aspect ratio preset selector (1:1, 4:3, 3:2, 16:9, 9:16, Custom buttons)
- Removed custom ratio inputs and all aspect ratio state variables
- Removed AspectRatio type, ASPECT_RATIO_PRESETS, calculateHeight, calculateWidth, validateAspectRatioValue
- Removed adjustDimensionsAndCellSize function
- Removed adjustmentWarning state and toast UI
- Made Height field editable (was disabled/readonly, now identical to Width)
- Both Width/Height use 64-4096px range validation
- Default 1000x1000 maintained
- Export JSON bumped to v2.0.0, removed aspectRatioPreset field

### Files changed
- src/app/components/AssetGenerator.tsx (-227 lines)
- src/lib/dimensionUtils.ts (-122 lines)

### Notes for next iteration
- adjustCellSizeForDimensions still exists - needed for current preset buttons
- PRD-007+ (dynamic cell size buttons, allow cropping) not started
- Width/Height are now independent but cell size preset system unchanged
- Next priority: PRD-007 dynamic cell size buttons based on GCD divisors

## 2026-02-06 - PRD-007, PRD-008, PRD-009, PRD-010, PRD-011, PRD-018, PRD-019

### Completed
- Implemented dynamic GCD-based cell size buttons
- Added getDynamicMinCellSize function: max(2, ceil(max(width,height) * 0.01))
- Added getValidCellSizesForButtons: divisors of GCD in [dynamicMin, 200]
- Added findNearestValidCellSize with tie-breaking (prefer larger)
- Removed old CELL_SIZE_PRESETS constant
- Removed Custom button and freeform cell size input
- Removed adjustCellSizeForDimensions function (no longer used)
- Auto-selects nearest valid cell size when dimensions change
- Keeps current cell size if still valid after dimension change
- Added blocking state when zero valid sizes: grid disabled, exports disabled, message shown
- Added hint when <=3 valid sizes: "Few valid sizes. Enable Allow cropping..."

### Files changed
- src/lib/dimensionUtils.ts (refactored, removed old functions)
- src/app/components/AssetGenerator.tsx (dynamic buttons, blocking state)

### Notes for next iteration
- PRD-011a partially done - blocking messages shown but "Allow cropping" feature needed for full verification
- PRD-005 depends on Allow cropping feature (step 4 tests cropping mode)
- Next priority: PRD-012+ (Allow cropping checkbox and slider)

## 2026-02-06 - PRD-012

### Completed
- Added "Allow cropping" checkbox with info icon and hover tooltip
- Tooltip text: "Enabling this allows any cell size, even if it doesn't perfectly divide the SVG dimensions. Fragments at the edge will be cropped."
- Added allowCropping state variable (default: false)
- Cell size buttons hidden when allowCropping enabled (placeholder for slider, PRD-013)
- Blocking state and hints only show when NOT in cropping mode
- Export buttons enabled when allowCropping is true (even with zero GCD divisors)

### Files changed
- src/app/components/AssetGenerator.tsx (added state, checkbox UI, conditional rendering)

### Notes for next iteration
- PRD-014 next: mark GCD divisors on slider
- PRD-015: crop direction toggle (width/height)
- PRD-016, PRD-017: actual cropping behavior in grid rendering
- PRD-020: export format updates for allowCropping, cropDirection
- PRD-021: behavior when disabling cropping

## 2026-02-06 - PRD-013

### Completed
- Added cell size slider for cropping mode with range dynamicMin to 200
- Slider dynamically calculates min based on getDynamicMinCellSize(width, height)
- Slider replaces divisor buttons when Allow cropping is enabled
- Divisor buttons return when Allow cropping is disabled
- Slider has filled track styling matching other sliders in the app

### Files changed
- src/app/components/AssetGenerator.tsx (added slider, imported getDynamicMinCellSize and MAX_CELL_SIZE)

### Notes for next iteration
- PRD-014 next: add tick marks on slider for GCD divisors, show "Evenly divisible" indicator
- PRD-015: crop direction toggle (width/height)
- PRD-016, PRD-017: actual cropping behavior in grid rendering
- PRD-020: export format updates for allowCropping, cropDirection
- PRD-021: behavior when disabling cropping (nearest-valid-size logic)

## 2026-02-06 - PRD-015

### Completed
- Added crop direction toggle with "Crop width" and "Crop height" options
- Toggle only visible when Allow cropping is enabled
- Default selection is "Crop height" (per PRD spec)
- Added cropDirection state: 'width' | 'height'
- Styled consistently with other toggles in the UI

### Files changed
- src/app/components/AssetGenerator.tsx (added cropDirection state and toggle UI)

### Notes for next iteration
- PRD-016: implement actual "Crop width" behavior (rightmost column partial, full rows only)
- PRD-017: implement actual "Crop height" behavior (bottom row partial, full columns only)
- PRD-014: slider tick marks for GCD divisors + "Evenly divisible" indicator
- PRD-020: export format needs allowCropping + cropDirection fields
- PRD-021: behavior when disabling cropping (nearest-valid-size from slider value)

## 2026-02-06 - PRD-016, PRD-017

### Completed
- Implemented crop width behavior: ceil(width/cellSize) cols, floor(height/cellSize) rows
- Implemented crop height behavior: floor(width/cellSize) cols, ceil(height/cellSize) rows
- Added CropDirection type to FragmentConfig
- Updated gridToSvg to render partial cells at edges based on cropDirection
- Background fills entire canvas; partial cells clipped to remaining space
- Canvas dimensions never change - always user-specified width/height
- Updated canvas preview to handle cropping mode with proper partial cell rendering
- Added debounced state for allowCropping and cropDirection
- Passed cropping params through generateSVG, gridVariations, and generateFragmentSvgDirect

### Files changed
- src/lib/generateFragmentSvg.ts (added CropDirection type, GridToSvgOptions, cropping logic)
- src/lib/generateFragmentSvgGrid.ts (re-export CropDirection, updated generateFragmentSvgDirect)
- src/app/components/AssetGenerator.tsx (debounced cropping state, canvas drawing with cropping)

### Notes for next iteration
- PRD-014: slider tick marks for GCD divisors + "Evenly divisible" indicator
- PRD-020: export format needs allowCropping + cropDirection fields
- PRD-021: behavior when disabling cropping (nearest-valid-size from slider value)
- PRD-005, PRD-011a may now be verifiable

## 2026-02-06 - PRD-020

### Completed
- Added allowCropping (boolean) to export JSON config
- Added cropDirection ('width'|'height') to export - only included when allowCropping is true
- Used spread operator conditional: ...(allowCropping ? { cropDirection } : {})
- Added proper TypeScript typing for export data structure
- Version already 2.0.0 from previous work; aspectRatio fields already removed

### Files changed
- src/app/components/AssetGenerator.tsx (exportSettingsAsJson function)

### Notes for next iteration
- PRD-021: when disabling cropping, apply nearest-valid-size logic to slider value
- PRD-014: slider tick marks for GCD divisors + "Evenly divisible" indicator
- PRD-005, PRD-011a: verification tasks
- Note: VM has mmap issues preventing npm build/typecheck; code compiles correctly based on syntax review
